declare module 'react-native-webrtc-web-shim' {
  import {
    mediaDevices,
    MediaStream,
    RTCIceCandidate,
    RTCPeerConnection,
    RTCSessionDescription
  } from 'react-native-webrtc'

  export {
    mediaDevices,
    MediaStream,
    RTCIceCandidate,
    RTCPeerConnection,
    RTCSessionDescription
  }
}
